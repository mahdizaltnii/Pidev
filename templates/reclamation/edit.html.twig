{% extends 'base-front.html.twig' %}

{% block title %}Edit Reclamation{% endblock %}

{% block body %}
<br><br><br><br><br><br><br><br><br><br>
{{ form_start(recform,{'attr': {'novalidate': 'novalidate'}} ) }}
    {{ form_widget(recform) }}
    <h1>Edit Reclamation</h1>

    {{ include('reclamation/_form.html.twig', {'button_label': 'Update'}) }}

    {{ form_end(recform) }}
    <a href="{{ path('reclamation_index') }}">back to list</a>

    {{ include('reclamation/_delete_form.html.twig') }}
{% endblock %}